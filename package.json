{
  "scripts": {
    "start": "parcel index.dev.html --no-cache --dist-dir dist --log-level info",
    "build": "parcel build index.html --public-url ./ --dist-dir dist --log-level info"
  },
  "devDependencies": {
    "parcel": "^2.1.0",
    "process": "^0.11.10",
    "prettier": "^2.8.8"
  },
  "dependencies": {
    "@auth0/auth0-spa-js": "^2.0.4"
  }
}
